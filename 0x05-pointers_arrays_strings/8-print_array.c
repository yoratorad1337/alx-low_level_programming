#include "main.h"

/**
* print array
* followed by a newline
* @str
*/
void print_array(int *a, int n)
{
	int i;
	char comm;

	com = ' ,';

	for (i = 0; i <= n; i++)
	{
		printf("%d%c ",a[i] ,comm);
	}
}
