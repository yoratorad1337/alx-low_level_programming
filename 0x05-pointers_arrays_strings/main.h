#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

int _putchar(char c);

/* 0.98 Battery */
void reset_to_98(int *n);

/*don't swap horses in crossing a stream*/
void swap_int(int *a, int *b);

#endif
